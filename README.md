# Score 7 Field Kit revocation

This repository contains only the signed emergency revocation update for
previously distributed Score 7 Field Kit installations. It does not contain
deployment tooling, device images, credentials, or operational scripts.

Running the update removes the installed deployment shell scripts and their
script-only rollback copies. It does not delete ROM images, partition backups,
reports, mtkclient, or unrelated user files.
